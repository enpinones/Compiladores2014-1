class AUX is
   not:STR is
      return false;
   end;
end;

class COND_ERRORS is
   main(x:INT) is
      if 1 then
	 #OUT+"uno";
      end;
      var2:STR:=false;
      if (1.is_zero and ~var2) or 5 < x then
	 #OUT + "dos";
      end;
   end;
end
